<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="/dist/highlight.min.js"></script>
    <link rel="stylesheet" href="/dist/atom-one-dark.min.css">
    <link rel="stylesheet" href="/style.css" />
    <script>
      hljs.highlightAll();
    </script>
    <title>DasteBin</title>
  </head>
  <body>
    <div class="wrapper">
      <%- include("_buttons.ejs", { canShowRaw: true, canDuplicate: true, cid: locals.cid }) %>
      <div class="line-numbers">
        <% for(let i = 1; i <= code.split("\n").length; i++) { %>
        <div><%= i %></div>
        <% } %>
      </div>
      <pre><code id="code-display" 
        class="<%= locals.language ? `language-${locals.language}` : "" %>"><%= code %></code></pre>
    </div>
  </body>
</html>
